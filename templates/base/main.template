package main

import (
	"%%PACKAGE_NAME%%/src/mks_modules"
	"%%PACKAGE_NAME%%/src/utils"
	"fmt"
)

func main() {
	config, err := utils.LoadConfig() // Getting the ENV Values
	if err != nil {
		fmt.Println("[-] Fatal Error!\nCannot load config file:", err)
		return
	}

	// Creating waitgroup
	mks_modules.WaitGroup = new(sync.WaitGroup)

	mks_modules.WaitGroup.Wait()
}